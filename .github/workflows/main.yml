name: Hello World

on:
  push: {branches: main}
  pull_request: {branches: main}

jobs:
  test:
    runs-on: ubuntu-latest 
    name: Install external repo
    steps:
      - uses: actions/checkout@v2
      - run: node index.js
